<UserControl x:Class="PrismApp.Views.LogOut"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:PrismApp.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:LogOutViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="10" Width="300">
        
        <TextBlock Margin="10" FontSize="30" Text="ログアウトしますか？" HorizontalAlignment="Center"/>
        
        <Button Margin="10" Width="80" Content="ログアウト" Command="{Binding LogOutCommand}"/>
        
        <Button Margin="10" Width="80" Content="戻る" Command="{Binding GoBackCommand}"/>

    </StackPanel>
</UserControl>
