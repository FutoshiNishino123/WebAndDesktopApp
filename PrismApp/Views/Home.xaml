<UserControl x:Class="PrismApp.Views.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:PrismApp.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" VerticalAlignment="Top" HorizontalAlignment="Center">
            <TextBlock FontSize="30" Text="ホーム" VerticalAlignment="Top" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding Description}" MaxWidth="300" TextWrapping="Wrap"/>
        </StackPanel>
        <TextBlock DockPanel.Dock="Bottom" Text="{Binding Version}" Height="30" HorizontalAlignment="Center" />
        <Button Content="ログイン" Width="50" Height="20" Command="{Binding NavigateCommand}" CommandParameter="LogIn"/>
    </DockPanel>
</UserControl>
