<UserControl x:Class="PrismApp.Views.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:PrismApp.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <TextBlock Text="ホーム" Height="40" FontSize="24" VerticalAlignment="Top" />
        <TextBlock Text="{Binding Description}" Margin="0,50,0,40" TextWrapping="Wrap"/>
        <TextBlock Text="{Binding Version}" Height="30" VerticalAlignment="Bottom" />
    </Grid>
</UserControl>
